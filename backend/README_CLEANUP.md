# Backend — cleanup summary

Документация и вспомогательные файлы были перемещены для уменьшения шума в корне `backend/`.

Что сделано:
- `backend/docs/` — все Markdown/справочные файлы.
- `backend/docs/phase_archive/` — архивные PHASE_ файлы.
- `backend/scripts_windows/` — вспомогательные скрипты (если применимо).

Что осталось в `backend/`:
- `src/`, `migrations/`, `node_modules/`, `.env`, `package.json`, `package-lock.json`, `setup-demo-data.js`, `storage/`.

Как найти документацию: откройте `backend/docs/`.
