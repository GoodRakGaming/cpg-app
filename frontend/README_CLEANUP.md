# Frontend — cleanup summary

Документация и вспомогательные файлы были перемещены для уменьшения шума в корне `frontend/`.

Что сделано:
- `frontend/docs/` — все Markdown/справочные файлы.
- `frontend/docs/phase_archive/` — архивные PHASE_ файлы.

Что осталось в `frontend/`:
- `app/`, `lib/`, `node_modules/`, `public/`, `package.json`, `next.config.ts`, `tsconfig.json`.

Как найти документацию: откройте `frontend/docs/`.
